import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.avaninder.allai',
  appName: 'allAIapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
